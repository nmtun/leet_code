class Solution {
public:
    int climbStairs(int n) {
        int ans=0;
        int prev1=1, prev2=0;
        for(int i=1; i<=n; i++){
            ans = prev1 + prev2;
            prev2 = prev1;
            prev1 = ans;
        }
        return ans;
    }
};
